module.exports = {
  theme: {
    container: {
      center: false, // 默认容器是否居中
      padding: '0rem', // 默认容器两边垂直边距
    },
    corePlugins: {
      container: true, // 是否启用container，测试时未生效
    },
  },
  variants: {},
  plugins: []
}
